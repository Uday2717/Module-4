Q3)How many except statements can a try-except block have? Name Some built-in exception classes.
Ans)We can have multiple try blocks in a program but only one except statement with each try block.
Built-in Exception Classes:-
Python Built-in Exceptions. Python has a number of built-in exceptions, such as SyntaxError, NameError, and TypeError. 